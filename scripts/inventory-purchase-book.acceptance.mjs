import fs from 'node:fs';
const app=fs.readFileSync(new URL('../src/App.jsx',import.meta.url),'utf8'); const api=fs.readFileSync(new URL('../src/lib/inventoryApi.js',import.meta.url),'utf8'); const page=fs.readFileSync(new URL('../src/pages/PurchaseBookPage.jsx',import.meta.url),'utf8'); const req=(s,v,m)=>{if(!s.includes(v))throw new Error(m||`Missing: ${v}`);};
req(app,'path="inventory/purchases" element={<PurchaseBookPage />}','Purchase Book route must be live.'); req(api,"centralApi.get('/v1/purchases'",'Purchase Book must use Central V1 list.'); req(api,'centralApi.get(`/v1/purchases/${encodeURIComponent(String(purchaseId))}`)','Purchase detail must use Central V1 detail.'); req(page,'Read-only purchase authority'); req(page,'Central filters by branch, supplier and received date.'); req(page,'Received batches'); if(page.includes('createPurchase(')||page.includes('updateProduct(')||page.includes('stock_quantity ='))throw new Error('Purchase Book must not mutate purchase or stock authority.'); if(page.includes('localStorage')||page.includes('indexedDB')||page.includes('posApi'))throw new Error('Purchase Book must not use browser/POS authority.'); console.log('RetailHub Inventory V1 Purchase Book acceptance passed.');
